// Turn-based battle over the paused world. Fleshed out in Phase 3.
import Phaser from "phaser";

export class BattleScene extends Phaser.Scene {
  constructor() {
    super("BattleScene");
  }

  create(): void {
    // Phase 3
  }
}
